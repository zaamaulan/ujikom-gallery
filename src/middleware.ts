export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // "/",
    // "/home",
    // "/gallery/:path*",
    // "/contact",
    // "/about",
    // "/liked-photo",
  ],
};
